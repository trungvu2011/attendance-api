spring:
  datasource:
    username: root
    password: chien0210
    url: jdbc:mysql://localhost:3306/attendance
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true